[package]
name = "microvms-js"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "the napi-rs bindings, populated by T-W3-8"

# Deliberately dependency-free. The crate exists so the workspace member list is
# written once; T-W3-8 adds what it needs.
[dependencies]
